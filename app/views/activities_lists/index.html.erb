<p id="notice"><%= notice %></p>

<h1>Activities Lists</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @activities_lists.each do |activities_list| %>
      <tr>
        <td><%= link_to 'Show', activities_list %></td>
        <td><%= link_to 'Edit', edit_activities_list_path(activities_list) %></td>
        <td><%= link_to 'Destroy', activities_list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Activities List', new_activities_list_path %>
